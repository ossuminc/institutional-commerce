user DemoAdmin is "The person that is administering the demo"
user DemoMember is "The person that is being enrolled during a demo so they can participate in a scenario"
user Member is "The person that is being enrolled in the app outside a demo"
context App is { ??? } with { briefly "The application the user is using to perform this action" }
context GatewayAction is {
 ???
} with {
   briefly "The interface that accepts the request for processing by the backend"
}
